steps to run before any playbook:

* install git
* install ansible
* git clone https://github.com/pwnage101/machine-configs.git

steps to run before server playbook:

* ssh-copy-id to the remote host
